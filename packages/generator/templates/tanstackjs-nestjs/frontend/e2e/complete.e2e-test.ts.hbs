/**
 * Comprehensive E2E Tests for {{project.name}}
 * Generated by ERDwithAI — Next.js + NestJS Stack
 *
 * Entities: {{#each entities}}{{name}}{{#unless @last}}, {{/unless}}{{/each}}
 *
 * Frontend: process.env.FRONTEND_URL (default: http://localhost:3000)
 * Backend:  process.env.BACKEND_URL  (default: http://localhost:3001)
 */

import { test, expect } from '@playwright/test';

// ============================================================================
// Configuration
// ============================================================================

const FRONTEND_URL = process.env.FRONTEND_URL || 'http://localhost:3000';
const BACKEND_URL  = process.env.BACKEND_URL  || 'http://localhost:3001';
const API_BASE     = `${BACKEND_URL}/api/bus`;

// Entity paths
{{#each entities}}
const {{upperCase (snakeCase name)}}_PATH = '/{{tableName}}';
{{/each}}

// ============================================================================
// Helpers
// ============================================================================

async function waitForPageReady(page: any) {
  await page.waitForLoadState('networkidle');
  await page.waitForTimeout(500);
}

// ============================================================================
// API Health Checks
// ============================================================================

test.describe('{{project.name}} — API Health', () => {
{{#each entities}}
  test('GET /api/bus/{{plural (snakeCase name)}} should respond 200', async ({ request }) => {
    const res = await request.get(`${API_BASE}/{{plural (snakeCase name)}}`);
    expect(res.status()).toBe(200);
    const data = await res.json();
    expect(data).toHaveProperty('data');
    expect(Array.isArray(data.data)).toBe(true);
  });

{{/each}}
});

// ============================================================================
// Entity CRUD Tests (via API)
// ============================================================================

{{#each entities}}
test.describe('{{displayName}} — CRUD', () => {
  test('should create a {{displayName}} via API', async ({ request }) => {
    const payload: Record<string, unknown> = {
    {{#each attributes}}
    {{#unless (isExcludedField name)}}
      {{name}}: {{mockValue type name}},
    {{/unless}}
    {{/each}}
    };

    const res = await request.post(`${API_BASE}/{{plural (snakeCase name)}}`, {
      data: payload,
      headers: { 'Content-Type': 'application/json' },
    });
    expect([200, 201]).toContain(res.status());
    const created = await res.json();
    expect(created).toHaveProperty('id');

    if (created.id) {
      await request.delete(`${API_BASE}/{{plural (snakeCase name)}}/${created.id}`);
    }
  });

  test('should read a {{displayName}} via API', async ({ request }) => {
    const payload: Record<string, unknown> = {
    {{#each attributes}}
    {{#unless (isExcludedField name)}}
      {{name}}: {{mockUniqueValue type name 0}},
    {{/unless}}
    {{/each}}
    };

    const createRes = await request.post(`${API_BASE}/{{plural (snakeCase name)}}`, {
      data: payload,
      headers: { 'Content-Type': 'application/json' },
    });
    const created = await createRes.json();

    if (created.id) {
      const getRes = await request.get(`${API_BASE}/{{plural (snakeCase name)}}/${created.id}`);
      expect(getRes.status()).toBe(200);
      await request.delete(`${API_BASE}/{{plural (snakeCase name)}}/${created.id}`);
    }
  });

  test('should update a {{displayName}} via API', async ({ request }) => {
    const createRes = await request.post(`${API_BASE}/{{plural (snakeCase name)}}`, {
      data: {
      {{#each attributes}}
      {{#unless (isExcludedField name)}}
        {{name}}: {{mockUniqueValue type name 1}},
      {{/unless}}
      {{/each}}
      },
      headers: { 'Content-Type': 'application/json' },
    });
    const created = await createRes.json();

    if (created.id) {
      const updateRes = await request.patch(`${API_BASE}/{{plural (snakeCase name)}}/${created.id}`, {
        data: {
        {{#each attributes}}
        {{#unless (isExcludedField name)}}
          {{name}}: {{mockUniqueValue type name 2}},
        {{/unless}}
        {{/each}}
        },
        headers: { 'Content-Type': 'application/json' },
      });
      expect([200, 201]).toContain(updateRes.status());
      await request.delete(`${API_BASE}/{{plural (snakeCase name)}}/${created.id}`);
    }
  });

  test('should delete a {{displayName}} via API', async ({ request }) => {
    const createRes = await request.post(`${API_BASE}/{{plural (snakeCase name)}}`, {
      data: {
      {{#each attributes}}
      {{#unless (isExcludedField name)}}
        {{name}}: {{mockUniqueValue type name 3}},
      {{/unless}}
      {{/each}}
      },
      headers: { 'Content-Type': 'application/json' },
    });
    const created = await createRes.json();

    if (created.id) {
      const delRes = await request.delete(`${API_BASE}/{{plural (snakeCase name)}}/${created.id}`);
      expect([200, 204]).toContain(delRes.status());
      const getRes = await request.get(`${API_BASE}/{{plural (snakeCase name)}}/${created.id}`);
      expect([400, 404]).toContain(getRes.status());
    }
  });
});

{{/each}}

// ============================================================================
// Frontend Page Tests
// ============================================================================

{{#each entities}}
test.describe('{{displayName}} — Frontend', () => {
  test('should display {{displayName}} list page', async ({ page }) => {
    await page.goto(`${FRONTEND_URL}/{{tableName}}`);
    await waitForPageReady(page);
    await expect(page.locator('h1, h2').first()).toBeVisible({ timeout: 10000 });
    await expect(page.locator('table').first()).toBeVisible({ timeout: 10000 });
    await expect(
      page.locator('button:has-text("Create"), button:has-text("New"), button:has-text("Add")').first()
    ).toBeVisible();
  });

  test('should show form fields on create {{displayName}} page', async ({ page }) => {
    await page.goto(`${FRONTEND_URL}/{{tableName}}/new`);
    await waitForPageReady(page);
    const form = page.locator('form').first();
    await expect(form).toBeVisible({ timeout: 10000 });
    const inputs = page.locator('input, select, textarea');
    expect(await inputs.count()).toBeGreaterThan(0);
  });

  test('should have save and cancel buttons on create {{displayName}} page', async ({ page }) => {
    await page.goto(`${FRONTEND_URL}/{{tableName}}/new`);
    await waitForPageReady(page);
    await expect(
      page.locator('button[type="submit"], button:has-text("Save"), button:has-text("Create")').first()
    ).toBeVisible({ timeout: 10000 });
    await expect(
      page.locator('button:has-text("Cancel"), button:has-text("Back"), a:has-text("Back")').first()
    ).toBeVisible({ timeout: 10000 });
  });
});

{{/each}}

// ============================================================================
// Navigation Tests
// ============================================================================

test.describe('{{project.name}} — Navigation', () => {
  test('should load home/dashboard without error', async ({ page }) => {
    await page.goto(FRONTEND_URL);
    await waitForPageReady(page);
    await expect(page).not.toHaveURL(/error/);
    await expect(page.locator('body')).toBeVisible();
  });

{{#each entities}}
  test('should navigate to {{displayName}} list', async ({ page }) => {
    await page.goto(`${FRONTEND_URL}/{{tableName}}`);
    await waitForPageReady(page);
    await expect(page.locator('h1, h2').first()).toBeVisible({ timeout: 10000 });
  });

{{/each}}
});

// ============================================================================
// Responsive Design
// ============================================================================

{{#with (lookup entities 0)}}
test.describe('{{../project.name}} — Responsive Design', () => {
  test('should be usable on tablet (768 × 1024)', async ({ page }) => {
    await page.setViewportSize({ width: 768, height: 1024 });
    await page.goto(`${FRONTEND_URL}/{{tableName}}`);
    await waitForPageReady(page);
    await expect(page.locator('h1, h2').first()).toBeVisible({ timeout: 10000 });
  });

  test('should be usable on mobile (375 × 812)', async ({ page }) => {
    await page.setViewportSize({ width: 375, height: 812 });
    await page.goto(`${FRONTEND_URL}/{{tableName}}`);
    await waitForPageReady(page);
    await expect(page.locator('body')).toBeVisible();
  });
});

// ============================================================================
// Error Handling
// ============================================================================

test.describe('{{../project.name}} — API Error Handling', () => {
  test('should return 404 for non-existent record', async ({ request }) => {
    const res = await request.get(
      `${API_BASE}/{{plural (snakeCase name)}}/00000000-0000-0000-0000-000000000000`
    );
    expect([400, 404]).toContain(res.status());
  });

  test('should return error for non-existent entity endpoint', async ({ request }) => {
    const res = await request.get(`${API_BASE}/nonexistententity`);
    expect([400, 404, 500]).toContain(res.status());
  });

  test('should handle malformed request body gracefully', async ({ request }) => {
    const res = await request.post(`${API_BASE}/{{plural (snakeCase name)}}`, {
      data: 'not-valid-json-object',
      headers: { 'Content-Type': 'application/json' },
    });
    expect([400, 422]).toContain(res.status());
  });
});
{{/with}}
