/**
 * SysRole Controller
 *
 * REST API controller for sys_role entity.
 *
 * Generated: {{now}}
 */

import { Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { SysRoleService } from '../services/sys-role.service';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';

@ApiTags('sys-role')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('sys/roles')
export class SysRoleController {
  constructor(private readonly sysRoleService: SysRoleService) {}

  @Get()
  @ApiOperation({ summary: 'List all sys_role records' })
  async findAll(
    @Query('page') page?: string,
    @Query('limit') limit?: string,
    @Query('isActive') isActive?: string,
  ) {
    return this.sysRoleService.findAll({
      page: page ? parseInt(page, 10) : undefined,
      limit: limit ? parseInt(limit, 10) : undefined,
      isActive: isActive ? isActive === 'true' : undefined,
    });
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get sys_role by ID' })
  async findOne(@Param('id') id: string) {
    return this.sysRoleService.findById(id);
  }

  @Get(':id/users')
  @ApiOperation({ summary: 'Get users with this role' })
  async getUsers(@Param('id') id: string) {
    return this.sysRoleService.getUsers(id);
  }

  @Get(':id/access')
  @ApiOperation({ summary: 'Get access permissions for this role' })
  async getAccess(@Param('id') id: string) {
    return this.sysRoleService.getAccess(id);
  }

  @Post()
  @ApiOperation({ summary: 'Create new sys_role' })
  async create(@Body() data: any) {
    return this.sysRoleService.create(data);
  }

  @Put(':id')
  @ApiOperation({ summary: 'Update sys_role' })
  async update(@Param('id') id: string, @Body() data: any) {
    return this.sysRoleService.update(id, data);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Partial update sys_role' })
  async partialUpdate(@Param('id') id: string, @Body() data: any) {
    return this.sysRoleService.update(id, data);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Delete sys_role' })
  async delete(@Param('id') id: string) {
    return this.sysRoleService.delete(id);
  }
}
