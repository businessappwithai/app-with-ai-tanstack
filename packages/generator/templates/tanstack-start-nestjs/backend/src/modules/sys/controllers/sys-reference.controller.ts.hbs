/**
 * SysReference Controller
 *
 * REST API controller for sys_reference entity.
 *
 * Generated: {{now}}
 */

import { Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { SysReferenceService } from '../services/sys-reference.service';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';

@ApiTags('sys-reference')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('sys/references')
export class SysReferenceController {
  constructor(private readonly sysReferenceService: SysReferenceService) {}

  @Get()
  @ApiOperation({ summary: 'List all sys_reference records' })
  async findAll(
    @Query('page') page?: string,
    @Query('limit') limit?: string,
  ) {
    return this.sysReferenceService.findAll({
      page: page ? parseInt(page, 10) : undefined,
      limit: limit ? parseInt(limit, 10) : undefined,
    });
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get sys_reference by ID' })
  async findOne(@Param('id') id: string) {
    return this.sysReferenceService.findById(id);
  }

  @Get(':id/ref-list')
  @ApiOperation({ summary: 'Get reference list values' })
  async getRefList(@Param('id') id: string) {
    return this.sysReferenceService.getRefList(id);
  }

  @Post()
  @ApiOperation({ summary: 'Create new sys_reference' })
  async create(@Body() data: any) {
    return this.sysReferenceService.create(data);
  }

  @Put(':id')
  @ApiOperation({ summary: 'Update sys_reference' })
  async update(@Param('id') id: string, @Body() data: any) {
    return this.sysReferenceService.update(id, data);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Partial update sys_reference' })
  async partialUpdate(@Param('id') id: string, @Body() data: any) {
    return this.sysReferenceService.update(id, data);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Delete sys_reference' })
  async delete(@Param('id') id: string) {
    return this.sysReferenceService.delete(id);
  }
}
