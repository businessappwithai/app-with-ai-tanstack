/**
 * SysUser Controller
 *
 * REST API controller for sys_user entity.
 *
 * Generated: {{now}}
 */

import { Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { SysUserService } from '../services/sys-user.service';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';

@ApiTags('sys-user')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('sys/users')
export class SysUserController {
  constructor(private readonly sysUserService: SysUserService) {}

  @Get()
  @ApiOperation({ summary: 'List all sys_user records' })
  async findAll(
    @Query('page') page?: string,
    @Query('limit') limit?: string,
    @Query('isActive') isActive?: string,
  ) {
    return this.sysUserService.findAll({
      page: page ? parseInt(page, 10) : undefined,
      limit: limit ? parseInt(limit, 10) : undefined,
      isActive: isActive ? isActive === 'true' : undefined,
    });
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get sys_user by ID' })
  async findOne(@Param('id') id: string) {
    return this.sysUserService.findById(id);
  }

  @Get(':id/roles')
  @ApiOperation({ summary: 'Get roles for a user' })
  async getRoles(@Param('id') id: string) {
    return this.sysUserService.getRoles(id);
  }

  @Get('email/:email')
  @ApiOperation({ summary: 'Get user by email (for authentication)' })
  async findByEmail(@Param('email') email: string) {
    return this.sysUserService.findByEmail(email);
  }

  @Post()
  @ApiOperation({ summary: 'Create new sys_user' })
  async create(@Body() data: any) {
    return this.sysUserService.create(data);
  }

  @Put(':id')
  @ApiOperation({ summary: 'Update sys_user' })
  async update(@Param('id') id: string, @Body() data: any) {
    return this.sysUserService.update(id, data);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Partial update sys_user' })
  async partialUpdate(@Param('id') id: string, @Body() data: any) {
    return this.sysUserService.update(id, data);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Delete sys_user' })
  async delete(@Param('id') id: string) {
    return this.sysUserService.delete(id);
  }
}
