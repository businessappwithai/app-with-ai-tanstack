/**
 * SysRefList Controller
 *
 * REST API controller for sys_ref_list entity.
 *
 * Generated: {{now}}
 */

import { Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { SysRefListService } from '../services/sys-ref-list.service';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';

@ApiTags('sys-ref-list')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('sys/ref-lists')
export class SysRefListController {
  constructor(private readonly sysRefListService: SysRefListService) {}

  @Get()
  @ApiOperation({ summary: 'List all sys_ref_list records' })
  async findAll(
    @Query('page') page?: string,
    @Query('limit') limit?: string,
    @Query('referenceId') referenceId?: string,
    @Query('isActive') isActive?: string,
  ) {
    return this.sysRefListService.findAll({
      page: page ? parseInt(page, 10) : undefined,
      limit: limit ? parseInt(limit, 10) : undefined,
      referenceId,
      isActive: isActive ? isActive === 'true' : undefined,
    });
  }

  @Get('reference/:referenceId')
  @ApiOperation({ summary: 'Get list values for a reference' })
  async getByReference(@Param('referenceId') referenceId: string) {
    return this.sysRefListService.getByReference(referenceId);
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get sys_ref_list by ID' })
  async findOne(@Param('id') id: string) {
    return this.sysRefListService.findById(id);
  }

  @Post()
  @ApiOperation({ summary: 'Create new sys_ref_list' })
  async create(@Body() data: any) {
    return this.sysRefListService.create(data);
  }

  @Put(':id')
  @ApiOperation({ summary: 'Update sys_ref_list' })
  async update(@Param('id') id: string, @Body() data: any) {
    return this.sysRefListService.update(id, data);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Partial update sys_ref_list' })
  async partialUpdate(@Param('id') id: string, @Body() data: any) {
    return this.sysRefListService.update(id, data);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Delete sys_ref_list' })
  async delete(@Param('id') id: string) {
    return this.sysRefListService.delete(id);
  }
}
