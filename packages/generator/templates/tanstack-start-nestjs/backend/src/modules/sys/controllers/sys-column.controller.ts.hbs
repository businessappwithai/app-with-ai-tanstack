/**
 * SysColumn Controller
 *
 * REST API controller for sys_column entity.
 *
 * Generated: {{now}}
 */

import { Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { SysColumnService } from '../services/sys-column.service';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';

@ApiTags('sys-column')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('sys/columns')
export class SysColumnController {
  constructor(private readonly sysColumnService: SysColumnService) {}

  @Get()
  @ApiOperation({ summary: 'List all sys_column records' })
  async findAll(
    @Query('page') page?: string,
    @Query('limit') limit?: string,
    @Query('tableId') tableId?: string,
  ) {
    return this.sysColumnService.findAll({
      page: page ? parseInt(page, 10) : undefined,
      limit: limit ? parseInt(limit, 10) : undefined,
      tableId,
    });
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get sys_column by ID' })
  async findOne(@Param('id') id: string) {
    return this.sysColumnService.findById(id);
  }

  @Post()
  @ApiOperation({ summary: 'Create new sys_column' })
  async create(@Body() data: any) {
    return this.sysColumnService.create(data);
  }

  @Put(':id')
  @ApiOperation({ summary: 'Update sys_column' })
  async update(@Param('id') id: string, @Body() data: any) {
    return this.sysColumnService.update(id, data);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Partial update sys_column' })
  async partialUpdate(@Param('id') id: string, @Body() data: any) {
    return this.sysColumnService.update(id, data);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Delete sys_column' })
  async delete(@Param('id') id: string) {
    return this.sysColumnService.delete(id);
  }
}
