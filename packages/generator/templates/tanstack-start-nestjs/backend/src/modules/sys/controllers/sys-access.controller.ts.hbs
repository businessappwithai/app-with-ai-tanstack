/**
 * SysAccess Controller
 *
 * REST API controller for sys_access entity.
 *
 * Generated: {{now}}
 */

import { Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { SysAccessService } from '../services/sys-access.service';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';

@ApiTags('sys-access')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('sys/access')
export class SysAccessController {
  constructor(private readonly sysAccessService: SysAccessService) {}

  @Get()
  @ApiOperation({ summary: 'List all sys_access records' })
  async findAll(
    @Query('page') page?: string,
    @Query('limit') limit?: string,
    @Query('roleId') roleId?: string,
    @Query('tableId') tableId?: string,
  ) {
    return this.sysAccessService.findAll({
      page: page ? parseInt(page, 10) : undefined,
      limit: limit ? parseInt(limit, 10) : undefined,
      roleId,
      tableId,
    });
  }

  @Get('role/:roleId')
  @ApiOperation({ summary: 'Get all access permissions for a role' })
  async getRoleAccess(@Param('roleId') roleId: string) {
    return this.sysAccessService.getRoleAccess(roleId);
  }

  @Get('check')
  @ApiOperation({ summary: 'Check if role has access to table' })
  async checkAccess(
    @Query('roleId') roleId: string,
    @Query('tableId') tableId: string,
  ) {
    return this.sysAccessService.checkAccess(roleId, tableId);
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get sys_access by ID' })
  async findOne(@Param('id') id: string) {
    return this.sysAccessService.findById(id);
  }

  @Post()
  @ApiOperation({ summary: 'Grant access permission' })
  async create(@Body() data: any) {
    return this.sysAccessService.create(data);
  }

  @Put(':id')
  @ApiOperation({ summary: 'Update sys_access' })
  async update(@Param('id') id: string, @Body() data: any) {
    return this.sysAccessService.update(id, data);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Partial update sys_access' })
  async partialUpdate(@Param('id') id: string, @Body() data: any) {
    return this.sysAccessService.update(id, data);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Revoke access permission' })
  async delete(@Param('id') id: string) {
    return this.sysAccessService.delete(id);
  }
}
