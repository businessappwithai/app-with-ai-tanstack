/**
 * SysTab Controller
 *
 * REST API controller for sys_tab entity.
 *
 * Generated: {{now}}
 */

import { Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { SysTabService } from '../services/sys-tab.service';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';

@ApiTags('sys-tab')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('sys/tabs')
export class SysTabController {
  constructor(private readonly sysTabService: SysTabService) {}

  @Get()
  @ApiOperation({ summary: 'List all sys_tab records' })
  async findAll(
    @Query('page') page?: string,
    @Query('limit') limit?: string,
    @Query('windowId') windowId?: string,
  ) {
    return this.sysTabService.findAll({
      page: page ? parseInt(page, 10) : undefined,
      limit: limit ? parseInt(limit, 10) : undefined,
      windowId,
    });
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get sys_tab by ID' })
  async findOne(@Param('id') id: string) {
    return this.sysTabService.findById(id);
  }

  @Get(':id/fields')
  @ApiOperation({ summary: 'Get fields for a tab (critical for dynamic UI)' })
  async getFields(
    @Param('id') id: string,
    @Query('view') view?: 'form' | 'grid',
  ) {
    return this.sysTabService.getFields(id, view);
  }

  @Post()
  @ApiOperation({ summary: 'Create new sys_tab' })
  async create(@Body() data: any) {
    return this.sysTabService.create(data);
  }

  @Put(':id')
  @ApiOperation({ summary: 'Update sys_tab' })
  async update(@Param('id') id: string, @Body() data: any) {
    return this.sysTabService.update(id, data);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Partial update sys_tab' })
  async partialUpdate(@Param('id') id: string, @Body() data: any) {
    return this.sysTabService.update(id, data);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Delete sys_tab' })
  async delete(@Param('id') id: string) {
    return this.sysTabService.delete(id);
  }
}
