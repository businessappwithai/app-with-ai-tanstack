/**
 * SysWindow Controller
 *
 * REST API controller for sys_window entity.
 *
 * Generated: {{now}}
 */

import { Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { SysWindowService } from '../services/sys-window.service';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';

@ApiTags('sys-window')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('sys/windows')
export class SysWindowController {
  constructor(private readonly sysWindowService: SysWindowService) {}

  @Get()
  @ApiOperation({ summary: 'List all sys_window records' })
  async findAll(
    @Query('page') page?: string,
    @Query('limit') limit?: string,
  ) {
    return this.sysWindowService.findAll({
      page: page ? parseInt(page, 10) : undefined,
      limit: limit ? parseInt(limit, 10) : undefined,
    });
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get sys_window by ID' })
  async findOne(@Param('id') id: string) {
    return this.sysWindowService.findById(id);
  }

  @Get(':id/tabs')
  @ApiOperation({ summary: 'Get tabs for a window' })
  async getTabs(@Param('id') id: string) {
    return this.sysWindowService.getTabs(id);
  }

  @Post()
  @ApiOperation({ summary: 'Create new sys_window' })
  async create(@Body() data: any) {
    return this.sysWindowService.create(data);
  }

  @Put(':id')
  @ApiOperation({ summary: 'Update sys_window' })
  async update(@Param('id') id: string, @Body() data: any) {
    return this.sysWindowService.update(id, data);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Partial update sys_window' })
  async partialUpdate(@Param('id') id: string, @Body() data: any) {
    return this.sysWindowService.update(id, data);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Delete sys_window' })
  async delete(@Param('id') id: string) {
    return this.sysWindowService.delete(id);
  }
}
