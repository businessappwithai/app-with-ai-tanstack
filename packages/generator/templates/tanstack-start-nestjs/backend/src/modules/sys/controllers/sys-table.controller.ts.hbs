/**
 * SysTable Controller
 *
 * REST API controller for sys_table entity.
 * Provides endpoints for managing Application Dictionary table definitions.
 *
 * Generated: {{now}}
 */

import { Controller, Get, Post, Put, Patch, Delete, Body, Param, Query, ParseIntPipe, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { SysTableService } from '../services/sys-table.service';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';

@ApiTags('sys-table')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@Controller('sys/tables')
export class SysTableController {
  constructor(private readonly sysTableService: SysTableService) {}

  @Get()
  @ApiOperation({ summary: 'List all sys_table records' })
  async findAll(
    @Query('page') page?: string,
    @Query('limit') limit?: string,
    @Query('search') search?: string,
    @Query('isActive') isActive?: string,
  ) {
    return this.sysTableService.findAll({
      page: page ? parseInt(page, 10) : undefined,
      limit: limit ? parseInt(limit, 10) : undefined,
      search,
      isActive: isActive ? isActive === 'true' : undefined,
    });
  }

  @Get(':id')
  @ApiOperation({ summary: 'Get sys_table by ID' })
  async findOne(@Param('id') id: string) {
    return this.sysTableService.findById(id);
  }

  @Post()
  @ApiOperation({ summary: 'Create new sys_table' })
  async create(@Body() data: {
    name: string;
    table_name: string;
    description?: string;
    table_type: 'system' | 'business';
    is_view?: boolean;
    access_level?: string;
    windowId?: string;
  }) {
    return this.sysTableService.create(data);
  }

  @Put(':id')
  @ApiOperation({ summary: 'Update sys_table' })
  async update(
    @Param('id') id: string,
    @Body() data: {
      name?: string;
      description?: string;
      is_active?: boolean;
      access_level?: string;
    },
  ) {
    return this.sysTableService.update(id, data);
  }

  @Patch(':id')
  @ApiOperation({ summary: 'Partial update sys_table' })
  async partialUpdate(
    @Param('id') id: string,
    @Body() data: {
      name?: string;
      description?: string;
      is_active?: boolean;
      access_level?: string;
    },
  ) {
    return this.sysTableService.update(id, data);
  }

  @Delete(':id')
  @ApiOperation({ summary: 'Delete sys_table' })
  async delete(@Param('id') id: string) {
    return this.sysTableService.delete(id);
  }

  @Get(':id/columns')
  @ApiOperation({ summary: 'Get columns for a table' })
  async getColumns(@Param('id') id: string) {
    return this.sysTableService.getColumns(id);
  }
}
